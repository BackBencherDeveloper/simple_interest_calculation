def simple_interest():
  p=int(input("enter the principal amount: "))
  r=int(input("enter the rate of interest: "))
  t=int(input("enter the time for calculation: "))
  si=(p*r*t)/100
  return si

#main
print(simple_interest())
